# frontend — local environment template.
# Copy to `.env.local` (git-ignored) and fill in if you need to point at a non-default API.

# Base URL of the spec-005 analytics read API. Defaults to http://localhost:8000/api/v1 if unset.
NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1
