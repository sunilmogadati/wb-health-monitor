name: 'wb_health_monitor'
version: '1.0.0'
config-version: 2
profile: 'wb_health_monitor'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]

# Zone discipline (constitution III): staging (source) -> warehouse (star) -> published (mart).
models:
  wb_health_monitor:
    warehouse:
      +schema: warehouse
      +materialized: table
    published:
      +schema: published
      +materialized: view
