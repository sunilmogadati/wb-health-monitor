# Alembic configuration.
#
# There is exactly one migration history with exactly one head at all times. Every later revision
# descends from the baseline root (0001), directly or transitively.

[alembic]
script_location = alembic
prepend_sys_path = .

# No sqlalchemy.url here, deliberately. The connection string is assembled in env.py from the same
# configuration the application uses, so a migration and the application can never disagree about
# which database they mean, and no credential is committed. A url in this file would be both a second
# source of truth and a secret in source.

# Date-prefixed revision filenames, so the directory listing sorts chronologically. Ordering is
# defined by down_revision, never by filename.
file_template = %%(year)d%%(month).2d%%(day).2d_%%(rev)s_%%(slug)s
timezone = UTC

# Fail rather than truncate. A silently shortened slug makes two revisions look alike in a listing.
truncate_slug_length = 60

[post_write_hooks]
# Format every generated migration with the same tool the rest of the backend uses.
hooks = ruff_format
ruff_format.type = exec
ruff_format.executable = ruff
ruff_format.options = format REVISION_SCRIPT_FILENAME

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
