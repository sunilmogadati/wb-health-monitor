# --- Secrets / local env (NEVER commit) ---
.env
.env.*
!.env.example

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.venv/
venv/
env/
dist/
build/

# --- Node / Next.js (stretch web service) ---
node_modules/
.next/
out/

# --- Editors / OS ---
.DS_Store
.idea/
*.swp

# --- local data exploration (not part of the project) ---
explore_wb.py
wb_health_sample.csv
scratch/

# --- generated data & model artifacts (regenerated via `make ingest` / `make train`) ---
backend/data/
backend/models/

# --- ML / vector-store binaries anywhere (regenerated from code; never commit) ---
*.joblib
chroma*/
*.sqlite3

# --- dbt artifacts (compiled SQL, run results, logs — regenerated by `make dbt-build`) ---
backend/dbt/target/
backend/dbt/logs/
backend/dbt/dbt_packages/

# --- per-developer volatile handoff (template is committed) ---
.specify/memory/session-state.md
